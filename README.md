# Multi_Agent_Research_System
There are 3 AI agents one has web-search tool, second has web-scraping tool and third writes the output

conda create -n langchain_agent python=3.12 -y
conda activate langchain_agent
pip install -r requirements.txt
